const $ = {
    oName = function(oer) {
        if (oer) {
            console.log('原始代码');
        };
    },
    oage = function(per) {
        if (per) {
            console.log('我是在dee-login分支创建出来的方法。');
        };
    },
};