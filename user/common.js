const $ = {
    oName = function(oer) {
        if (oer) {
            console.log('原始语句。');
        };
    },
    oGender = (per) => {
        if (per > 20 && per < 30) {
            console.log('这是在master分支中创造出的分支。');
        };
    },
    oage = function(per) {
        if (per) {
            console.log('我是在dee-login分支创建出来的方法。');
        };
    },
};