const $ = {
    oName = function(oer) {
        if (oer) {
            console.log('原始语句');
        };
    },
    oGender = (per) => {
        if (per > 20 && per < 30) {
            console.log('这是在master分支中创造出的分支。');
        };
    },
};